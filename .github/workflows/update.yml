name: Curate Links

on:
  schedule:
    - cron: '0 8 * * *'    # 8:00 AM UTC  (4am ET / 1am PT)
    - cron: '0 20 * * *'   # 8:00 PM UTC  (4pm ET / 1pm PT)
  workflow_dispatch:        # also triggerable manually from the Actions tab

permissions:
  contents: write           # needed to push updated index.html

jobs:
  curate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run curator
        run: python curator.py
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Commit updated page
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          # Only commit if index.html actually changed
          git diff --staged --quiet || git commit -m "Refresh links $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
